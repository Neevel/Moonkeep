{
  "name": "moonkeep-security-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-moonkeep \"node --test --test-concurrency=1 test/firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
